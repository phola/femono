import React from 'react'
import { mount } from 'enzyme'

import { Button } from '.'

describe('Components', function () {
  it('button component exists', function () {
    expect(Button).toBeDefined()
  })
})
